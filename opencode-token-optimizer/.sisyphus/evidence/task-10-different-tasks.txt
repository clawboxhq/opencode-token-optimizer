T10 Evidence: Different tasks pass through
==========================================
Ran: bun test src/__tests__/tools.test.ts
Result: 29 pass, 0 fail
Key tests:
- trackTask > registers a new task and returns duplicate: false ✓
- trackTask > allows different task IDs with the same description (not duplicates) ✓
- checkDuplicate > returns false for unrelated tasks ✓
- checkDuplicate > returns isDuplicate: false on an empty registry ✓
- checkDuplicate > returns false for empty query ✓
